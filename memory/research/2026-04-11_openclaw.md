# OpenClaw Research — 2026-04-11

Three findings from the latest GitHub releases and changelog summaries:

1. **2026.4.10 (latest)** added a bundled Codex provider with native threads and model discovery, plus an optional "Active Memory" plugin that lets agents recall relevant preferences and past context automatically without users manually triggering memory search.

2. **2026.4.9** introduced Dreaming with REM backfill and a new Diary timeline UI, alongside SSRF and node exec injection hardening to tighten security boundaries.

3. **Security focus across Q1–Q2 2026** has emphasized blocking browser SSRF redirect bypasses, sanitizing node exec events before enqueueing them as system events, and requiring owner authorization for allowlist mutations.
